version: '3'

services:
    castaway_1:
        build: .
        ports:
          - 58051:58051
        volumes:
          - .:/usr/src/castaway
        command: target/debug/castaway --tls --port 58051 --server localhost:58052
    castaway_2:
        build: .
        ports:
          - 58052:58052
        volumes:
          - .:/usr/src/castaway
        command: target/debug/castaway --tls --port 58052 --server localhost:58051
